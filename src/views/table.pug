table
  thead
    tr
      th(title="Name and abbreviation of the coin.") Coin Name
      th(title="The corresponding algorithm on NiceHash.") Algorithm
      th(title="Average cost to buy hashing power for this coin on NiceHash. It is possible to have good orders for less than this number.") Price / Cost
      th(title="Estimated rewards for mining that coin. Actual results may vary greatly. Hover over the number for a timestamp. Red text means old/lagging data.") Revenue / Rewards
      th(title="Revenue minus cost.") Profit
      th(title="Average returns on any bitcoin put into mining this coin on NiceHash. Actual results may vary greatly.").highlight ROI
  tbody
    each coin in data.coins
      tr(class=coin.isMostProfitable ? "highlight" : "")
        td= coin.displayName
        td= coin.algoName
        td= coin.price
        td(title=(new Date(coin.revenueDate)).toISOString() + " (UTC)" class=coin.lagging ? "lagging" : "").utc-title= coin.revenue
        td= coin.profit
        td(class=coin.isProfitable ? "cell-profitable" : "cell-not-profitable")= coin.roi
